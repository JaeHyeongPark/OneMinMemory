import { BlurMode } from './BlurMode';
export declare class Blur {
    radius: number;
    mode?: BlurMode | undefined;
    constructor(radius: number, mode?: BlurMode | undefined);
    toMap(): Record<string, any>;
}
